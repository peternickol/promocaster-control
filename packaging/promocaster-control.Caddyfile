http://__SITE__ {
    redir https://__SITE__{uri} permanent
}

__SITE__ {
    import __BASIC_AUTH_CONFIG__
    reverse_proxy __UPSTREAM__ {
        header_up X-Promocaster-User {http.auth.user.id}
    }
}
